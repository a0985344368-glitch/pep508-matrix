name: Synthetic CI

on:
  push:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - run: python -m unittest
